# Spring Data Source ?? - MySQL DB?? ??? ??

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/examplesdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: Yooshin1004!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
